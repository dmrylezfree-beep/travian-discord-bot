name: Defence Bot

on:
  workflow_dispatch:
    inputs:
      action:
        description: "Legacy action input"
        required: false
        default: "message"
      chat_id:
        description: "Legacy chat ID"
        required: false
        default: ""
      message_id:
        description: "Legacy message ID"
        required: false
        default: ""
      thread_id:
        description: "Telegram thread ID"
        required: false
        default: "38636"
      user_id:
        description: "Legacy user ID"
        required: false
        default: ""
      username:
        description: "Legacy username"
        required: false
        default: ""
      first_name:
        description: "Legacy first name"
        required: false
        default: ""
      message_text:
        description: "Legacy message text"
        required: false
        default: ""
      callback_id:
        description: "Legacy callback ID"
        required: false
        default: ""
      callback_data:
        description: "Legacy callback data"
        required: false
        default: ""

  schedule:
    - cron: "*/10 * * * *"

permissions:
  contents: write

# Only one bot session may exist at a time.
# New scheduled/manual runs are NOT allowed to pile up behind it.
concurrency:
  group: defence-bot
  cancel-in-progress: true

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install requests

      - name: Disable Telegram webhook
        env:
          DEFENCE_TELEGRAM_TOKEN: ${{ secrets.DEFENCE_TELEGRAM_TOKEN }}
        run: |
          python - <<'PY'
          import os
          import requests

          token = os.environ["DEFENCE_TELEGRAM_TOKEN"]
          response = requests.post(
              f"https://api.telegram.org/bot{token}/deleteWebhook",
              data={"drop_pending_updates": "false"},
              timeout=40,
          )
          response.raise_for_status()
          result = response.json()
          print(result, flush=True)
          if not result.get("ok"):
              raise SystemExit(1)
          PY

      - name: Run defence bot continuously
        env:
          DEFENCE_TELEGRAM_TOKEN: ${{ secrets.DEFENCE_TELEGRAM_TOKEN }}
          DEFENCE_RUN_SECONDS: "510"
          DEFENCE_TG_POLL_TIMEOUT: "20"
        run: python defence_poll.py
